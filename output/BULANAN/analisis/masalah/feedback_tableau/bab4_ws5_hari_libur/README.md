# Upload Bukti — Bab 4 WS5 Hari Libur (Opsional)

Folder ini opsional — kamu hanya perlu upload kalau setelah baca **Solusi #4** di `solusi_masalah.md` kamu masih bingung.

Kalau bingung, upload:
1. **SS_setup.png** — Screenshot setup WS5 sekarang (Columns, Rows, Marks card, Filters).
2. **describe_trend_model.txt** — Copy hasil "Describe Trend Model" (klik kanan trend line → Describe Trend Model → copy paste isinya ke .txt).

Aku akan cek apakah angka R²/slope-mu match dengan output Python `metrics.txt` di `bab_4_robustness/ws5_hari_libur/`.
